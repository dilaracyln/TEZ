extends layouts/main-layout.pug

block content
    // yeni mesaj oluştururken çıkan arama kısmı için:
    .chatPageContainer
        .chatTitleBar
            label(for="userSearchTextbox") Kime:
            #selectedUsers
                input#userSearchTextbox(type="text", placeholder="Kişinin adını yazın")
        .resultsContainer
        button#createChatButton(disabled) Sohbet oluşturun
